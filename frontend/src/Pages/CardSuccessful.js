import '../App.css'

/**
 * This is the page that is rendered when the user has successfully paid for their order.
 * @returns {JSX.Element} - The successful payment page.
 */
function Menu() {
  return (
    <div className='App'>
      <header className='App-header'>
        <p1>Payment successful</p1>
      </header>
    </div>
  )
}

export default Menu